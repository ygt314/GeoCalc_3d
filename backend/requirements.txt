# 3D几何计算器 后端依赖
pywebview>=6.0
sympy>=1.14.0

# GUI 后端(与 2D 原版一致:PyQt5 + QtWebEngine,Chromium 87)
# 注意:Chromium 87 不支持 ES2022 API,前端 index.html 已加 polyfill
PyQt5==5.15.11
PyQtWebEngine==5.15.7
qtpy>=2.4

# 运行需要 DISPLAY 环境变量(WSLg 下:DISPLAY=:0 QT_QPA_PLATFORM=xcb)
